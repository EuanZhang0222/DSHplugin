# 给安装与维护 Agent（智能体）的入口

1. 先完整阅读 [INSTALL.md（安装、升级与验收）](INSTALL.md)，当前版本与包摘要以 [releases/manifest.json（发布清单）](releases/manifest.json) 为准。
2. 这是 DeepSeek Harness（DSH）的插件仓库，不是 Codex 插件，也不能作为技能直接导入。目标机器必须已有可启动的 DSH；确认目标 DSH_HOME（数据目录）、profile（运行配置）和启动命令。
3. 使用 releases/ 中的预构建包，无需编译界面。不要选择各子目录中的历史 tgz 包；不要假设包已发布到 npm 公共仓库。
4. 升级前备份目标机器当前插件及设置，退出运行中的任务，再按 INSTALL.md 安装。不要复制开发者的 .dsh、数据库连接、凭据、会话或本机路径。
5. 已通过手工 insert（插入配置）安装的插件，先核对对应条目，迁移至包管理方式时只移除同名旧条目，避免重复加载。不得覆盖整份 cordis.patch.yml（配置补丁）。
6. 安装后必须重启实际目标宿主并刷新页面，核对实际解析路径、版本、页面和工具调用。网页接口测试成功不等于智能体工具注册成功。
7. API（接口）超长整数使用字符串传给工具；数据库分页连续读取 nextCursor（下一页游标）直到 complete（已完整读取），处理失败、预算不足和行分片。真实凭据只能在目标环境配置。
8. 修改代码后运行相关现有测试，重新打包并更新摘要；不要把仅有设计原型的能力写成已实现。长期工作在任务目录保存进度与下一步。
