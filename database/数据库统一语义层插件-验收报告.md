# 数据库统一语义层插件 2.1.0——验收报告

## 最新补充验收：2026-08-28 数据集全量查询与连续分页

- 自动化：82项核心测试和5项既有语义/模拟模型运行时测试通过。覆盖0/1/200/201/1507条、重复数据/无主键、查询期间源数据变化、350字段、超长中文与表情无损分片、游标防篡改/会话隔离、权限变化、失败/超时/取消/过期/资源不足，以及固定页边界/随机跳页/页数/越界输入/页内分片装配。
- 完整分页器接口：MySQL与ClickHouse各1507条，每页200共8页；直接跳第8页返回107条，再跳第3页返回401～600条；每页50共31页，最后7条。超范围页码拒绝；查询次数保持1次，跳页不重查数据库。
- MySQL与ClickHouse：使用真实数据库驱动连接本机协议测试服务，智能体工具与HTTP接口均各取回1507条，工具默认文本预算下各10页；唯一标识集合完整，大整数和小数精度保持，敏感字段排除。另验证筛选和倒序99条。这里不是原生数据库引擎性能压测，也不是正式付费模型执行。
- 浏览器：数据集详情“浏览数据”从第1页翻到第8页（每页200），最后显示共1507条/已读取至最后一页且禁用下一页；上一页、刷新重新查询、页大小、关闭释放通过。ClickHouse第2页正常。
- 样式：浅色/深色截图检查；实际CSS视口宽2851/1910/1134/687px，无页面或弹窗正文横向溢出。最终数字分页器在687px宽可见完整跳转操作；MySQL直接跳8/3页、50行31页及越界提示、ClickHouse深色输入跳8页通过。浏览器功能错误0；隔离服务主动重启期间有5条预期的连接重试警告，非分页功能异常。正式新页错误/警告0。
- 正式2.1.0已安装并在3080重启生效。升级前后均为2连接、3数据集、36字段、语义版本16，完整语义与公开连接哈希一致；其他插件注册清单保持。正式能源基础信息表实际查询返回9条，完整分页器显示共9条/1页，能碳皮肤正常。新页验证没有修改原用户页签和表单。
- 安装包核心测试82项通过，正式安装代码的5项语义/模拟模型测试通过。隔离新增的2个测试数据集和2连接已定点清理，原6个隔离数据集保留；本轮未发起正式模型付费调用。
- 完整性：只有采集成功并且游标读到末尾才返回complete=true。失败、超预算或游标过期不能当作全部完成；单次结果固定，不能拼接不同查询的页来冒充同一结果。
- 明确边界：最多8个保留查询、缓存合计1GiB、采集超时10分钟、空闲1小时过期；超过资源边界会明确失败，需要按互不重叠条件分批。仍遵守数据库权限和字段门禁，不承诺无限上下文、无限磁盘或跨库事务快照。手写SQL诊断入口仍保留200行/15秒。

本轮证据：`C:\工作\deepseektest\插件测试证据\database_全量分页_20260828`。自动化使用测试数据，无正式业务写入。以下为历史验收，不应将旧版本总行数约束或旧测试数量套用到2.1.0数据集分页接口。

## 最新补充验收：2026-08-27 按需语义上下文

- 自动化：59项核心测试通过；另5项隔离安装运行时/真实工具注册测试通过，模型生成服务使用模拟器，不消耗正式模型额度。
- 协议集成：本地 MySQL 驱动完成真实握手、读取元数据、建集、规则识别/确认及参数化查询；2数据集、8字段、1确认关系、1查询行，通过。没有修改正式业务数据库。
- 新API：目录/字段分页、任务语义、分批计划、非法范围400、只读请求不改状态等6项通过。
- 大规模边界：90表/540字段虚拟规划4005批，验证最后表未被丢弃；两个65字段表的4225种跨表字段组合完整覆盖。单字段超长明确阻断，不默默截断。
- 界面：缺少问题提示、按拓扑检索、确认关系、补选字段重新生成、9数据集目录翻页、完整字段搜索、正式18字段分页、两模式切换保留问题与结果通过。2000预算下约6503的临时语义显示超限且未发送残缺内容。
- 皮肤：隔离浅色/深色及正式能碳皮肤截图已检查。当前实测页面宽1910px，语义弹窗1040×820px，正文无横向溢出；本轮未伪报移动端实测。沿用原响应式规则。
- 正式升级：先确认20个会话均非运行状态；更新插件两半并重启3080，保留其他插件和用户原标签页。旧连接2个、数据集1个、字段18个、语义版本10及完整语义/公开连接哈希在升级前后完全一致。
- 实例对比：“能源单位和排放因子”问题，原完整公开上下文3901字符，任务返回封套1802字符，减少约54%；本次选12字段，完整18字段仍可查看。该值是这一问题的字符量对比，不是通用压缩率或精确Token计费节省承诺。
- 页面错误：隔离及正式浏览器错误日志均0。验收临时新增3个数据集已定点删除，隔离原6数据集保留。临时资料只用于测试，不进入正式数据。
- 不宣称已验证：正式模型的真实生成质量/付费调用、所有上层插件已迁移、任意大型数据库查询性能及整个宿主会话永不超窗。新接口与预算逻辑可用；旧调用方继续自行拼完整上下文时仍需迁移。

证据：`C:\工作\deepseektest\插件测试证据\database_语义按需上下文_20260827`。包含API结果、正式前后哈希与字符对比、6张页面截图、运行日志。以下为历史验收记录，旧测试数量及旧网络结果不代表本轮状态。

验收日期：2026-08-25  
验收环境：DeepSeek Harness web profile（网页运行配置），独立验证端口 `3081`  
被验收包：`@deepseek-ai/dsh-database-connections`（数据库连接与统一语义层插件）2.0.0

## 1. 验收结论

插件已完成宿主加载、网页交互、规则引擎、语义发布、受控查询、兼容升级和安装包级验证。自动识别结果只生成候选；只有人工确认关系进入其他插件和 Agent（智能体）可读取的语义上下文。

现有两条 UAT（用户验收测试）数据库连接在本次验收网络环境中分别返回“服务端关闭连接”和“socket hang up（套接字连接中断）”，因此本报告不把它们标记为数据库连通。数据库执行链改用仅监听本机的 MySQL 协议测试服务完成，覆盖真实驱动握手、元数据读取、参数化查询和结果解析，不以模拟函数替代数据库驱动。

## 2. 自动化结果

### 2.1 核心规则测试

13 项通过、0 项失败。当前受限沙箱禁止 `node:test`（Node 测试器）创建子进程，因此使用同一 Node 24 运行时逐文件执行三组测试；测试断言与包内 `npm test` 完全相同。覆盖：

- 100 分可量化规则及阈值；
- 数据库外键确定性候选；
- 类型硬阻断与唯一性基数判定；
- 脱敏值覆盖证据；
- MySQL `?` 参数和 ClickHouse 强类型参数；
- 字段白名单、敏感字段拒绝和最多 200 行；
- 语义快照不含凭据、敏感字段或未确认关系；
- 大模型输出只能引用当前拓扑字段白名单。

### 2.2 协议级集成测试

结果：

```json
{
  "ok": true,
  "datasets": 2,
  "fields": 8,
  "confirmedRelations": 1,
  "queryRows": 1,
  "semanticVersion": 7
}
```

该流程实际执行：读取两张表及字段/注释/索引/外键，创建统一语义概念和两个数据集，创建拓扑，识别 `meter_reading.meter_id -> meter.id`，人工确认，生成只含确认关系的语义上下文，并用结构化条件查询一行真实协议数据。

### 2.3 Harness 宿主接口

- 插件 2.0.0 在独立 3081 端口加载成功；
- 连接列表只返回 `hasUsername/hasPassword`（是否保存用户名/密码），无 `username/password` 属性；
- 规则版本为 `relation-rules/1.0.0`，六项权重合计 100；
- Harness 返回一个可用模型服务提供方和两个模型；
- 宿主日志无插件加载失败、工具重名或配置校验错误。

正式 3080 运行副本同步后再次验证：语义状态接口返回 HTTP 200，语义结构版本为 1；连接接口返回 2 条原有 UAT（用户验收测试）连接且没有 `username/password`（用户名/密码）属性；“数据集”页的“新建数据集”按钮为可用状态。

大模型识别没有在验收中发起付费模型请求；已验证真实模型服务发现、提示词构建、输出 JSON 校验、端点白名单、确定性复核和候选落库代码路径。正式环境首次调用仍需由管理员选择模型并确认费用与数据治理策略。

## 3. 浏览器验收

真实 Harness + In-app Browser（应用内浏览器）验收通过，并已在相同视口将原型与真实插件截图共同对照。

- “连接管理 / 数据集 / 关系拓扑”三个页签存在；
- MySQL 和 ClickHouse 使用统一的连接操作；
- 编辑连接时用户名、密码均为空，不回显保存值；
- 数据集可选择来源连接、数据库和表，可填写表用途并读取真实字段；
- 未读取元数据前不展示伪造字段；读取后字段类型独立成列；
- 支持统一语义概念和可切换数据集的语义上下文预览；
- 支持多个拓扑、规则识别、大模型辅助、人工添加和人工确认；
- 数据库插件面板扩展至最大 1500 像素，视觉层级、三页签、统计卡、数据表格和语义提示区与原型保持一致；
- 浅色、深色与跟随系统皮肤均使用 Harness 主题变量；小视口通过响应式单列和局部表格滚动避免页面级横向溢出；
- 真实浏览器完成连接测试、两次新建数据集、字段元数据读取、拓扑创建、规则识别、人工确认和上下文预览；
- 已修复语义接口 404 导致连接列表一同丢失、“新建数据集”被静默禁用的问题，并在版本错位时显示明确重启提示。
- 正式“我的插件 → 数据库连接”入口已显示还原后的连接卡、统一操作栏、数据集页和关系拓扑页；MySQL 与 ClickHouse 均提供浏览、测试、创建数据集和更多操作。

## 4. 安全与兼容结论

- 创建后用户名和密码均不返回浏览器，也不进入导出包；
- 编辑时凭据留空表示保留旧值；
- 旧 1.0.0 连接设置可自动补充语义层默认字段；
- 数据集引用连接时禁止直接删除连接；
- 其他插件通过 `databaseSemanticLayer`（数据库语义层）服务调用，Agent 通过四个静态工具调用，不直接读取内部 settings（设置）；
- 查询不接受原始 SQL（结构化查询语言），只接受字段白名单内的结构化请求；
- 跨数据库关系提供语义与查询路径，不伪装为数据库原生跨库联表执行。

## 5. 生产使用说明

生产环境应使用只读数据库账号，先在非生产库完成连接与大模型候选人工复核，再逐步启用数据集。最终安装包文件清单与 SHA-256（安全哈希）记录在根目录交付清单中。

## 6. 2026-08-27 布局修订验收

连接管理和数据集已取消 1250 像素最大宽度，与关系拓扑统一跟随插件内容区宽高。卡片、工具栏和表单按内容区而非浏览器视口宽度响应；长内容由宿主纵向滚动，宽表仅局部横向滚动。

正式 3080 页面完成 1681×860、1231×760、731×560、681×460、381×360、1681×460 六档弹窗测试及浅色/深色验证；无页面级横向溢出，关闭重开尺寸记忆正常。原有 2 条连接未修改，13 项核心语义测试通过。本轮未重复执行真实数据库连通测试或付费模型调用。详细尺寸测量见 `design-qa.md`（设计质量验收）。

## 7. 2026-08-27 数据来源逐级选择验收

### 功能及自动化

来源数据库和来源表由文本输入改为真实目录下拉选择：打开新建数据集自动列库，选库列表，选表自动读取字段类型、原始注释与表注释。支持默认库、单库自动展开和浏览数据页来源预选；多个库时由用户选择，不擅自选择第一张表。

25 项测试通过、0 项失败：原语义上下文/查询/关系规则 13 项，新增来源目录 10 项和连字符标识符兼容/危险输入拒绝 2 项。目录测试覆盖默认和多库行为、空库、非法选择、字段读取失败重试、目录刷新、被删除的表、连续切换旧响应、取消及同表语义保留。新实现每次切换取消旧请求并校验响应版本，保存按钮只接受当前已读取成功的来源。

### 实际浏览器及协议

- 独立 3081 环境通过 MySQL 驱动、ClickHouse 客户端访问仅监听本机的协议测试服务，真实执行目录/字段接口；分别成功创建 5 字段和 3 字段测试数据集。
- 同一张表刷新后保留手工业务名称及自定义注释；切换连接、数据库或表后清除旧字段，不混用上一次来源。
- ClickHouse 空库明确提示；停止服务产生真实连接失败，页面显示错误及重试按钮、禁止保存；恢复服务后重试成功并自动读取字段。
- 正式“我的插件”窄弹窗内容宽 487 像素时三个来源选择单列排列，无页面级横向溢出；浅色/深色均检查，最后恢复原弹窗尺寸。
- 浏览器捕获错误为 0；完整 MySQL 协议集成再次通过，结果仍为 2 个数据集、8 个字段、1 条人工确认关系、1 行受控测试查询。

### 正式连接及边界

2026-08-27 正式 MySQL 连接可访问，返回 3 个库。`ems_betai` 列出 484 张表；带连字符的 `topv-prod` 列出 16 张表，选择 `devices_default` 自动显示 5 个字段和独立字段类型列。因此本节结论更新了 8 月 25 日当时网络不可达的记录；不表示正式 ClickHouse 连通性也已验证。

本轮只读取正式库表结构，未读取业务数据行、创建正式数据集或修改原连接；正式接口仍返回 2 条连接、0 个数据集，不返回用户名/密码属性。正式副本已更新并重启宿主，未调用付费大模型服务。连字符兼容保持严格标识符校验和分段引用；含其他未支持字符的标识符仍明确拒绝，不通过拼接绕过安全限制。

证据目录：`C:\工作\deepseektest\验证记录\数据库级联选择_20260827`。开发前插件与安装包备份见根目录交付清单。

## 8. 2026-08-27 必填与保存提示修订验收

名称、来源连接、来源数据库、来源表明确标注“* 必填”，名称最多 200 字符。缺项不再让按钮直接变灰：点击保存后集中列出问题、标红输入框、定位首个问题；点击提示可再次定位，补填后已解决提示实时消除。元数据加载、失败、来源失效分别解释原因并阻止请求，不把它们伪装成用户未填写。

自动化 34 项全部通过，其中保存校验新增 9 项：空名称/空白名称、四项汇总、200 字符边界、选填为空、输入后重算、目录/字段加载和失败、旧来源或缺字段、删除的连接/库/表、已有数据集编辑校验。后端原有安全和持久化约束不变。

隔离 3081 浏览器经 MySQL 真实驱动成功保存一条 3 字段测试数据集：表用途和字段业务语义均为空，证明未增加隐含必填项。新建和编辑只缺名称时均提示名称；停止本机测试数据库后保存失败，名称和用途保留，服务恢复后重试更新原数据集成功，总数仍为 5（原测试数据 4 条、本轮新增 1 条）。

正式页面通过 1681×860、1231×860、731×860 三档弹窗测试；浅色及隔离环境深色检查通过，无页面级横向溢出，浏览器捕获错误为 0。正式副本仅更新两个界面文件，宿主未重启，原有 2 条连接、0 个数据集、0 个拓扑未改变；本轮没有执行正式数据集保存或付费模型调用。用户原来的未保存页面未刷新，验收使用独立新页。

证据目录：`C:\工作\deepseektest\验证记录\数据库保存校验_20260827`，包括缺项截图、失败保留截图、三档布局测量及浏览器错误记录。安装包/源文件包及回滚备份见根目录交付清单。

## 9. 2026-08-27 数据库注释一键复制验收

- 新增两目标复制规则11项，累计45项测试全部通过。覆盖空白填充、保留人工内容、显式覆盖、空原注释不清空、相同值、200/5000字符限制、多行处理、不可变数据和只允许两列目标；不改字段类型、启用/敏感状态及统一概念。
- 隔离3081经真实MySQL驱动读取本机协议测试库，验证两列各复制2项并保留1项人工值；覆盖先显示数量，取消无修改，确认只更新目标列。复制后手动编辑、重新读取字段保留、保存并重新打开均通过。
- 隔离新建1条3字段测试数据集，总数从5变为6；复制按钮本身没有保存，保存按钮才创建记录。上下文接口返回保存后的业务名称和注释，并仍排除未启用及敏感测试字段。切换到其他表清空旧字段和结果、恢复默认仅填空白模式。
- 正式环境独立新页只读 `ems_betai.energy_info` 结构，18字段中17项有注释可复制，`id` 无原注释保持空白，不复制“数据库未提供”占位文字。两列入口均通过，随后返回列表丢弃本次测试草稿，未保存正式数据集或读取业务数据行。
- 正式弹窗1681×860、1231×860、731×860，内容宽1437/987/487像素，滚动宽度均相同；窄窗口按钮自然换行。隔离深浅色通过，两个测试页浏览器错误为0，最终恢复原尺寸。
- 正式仅同步两个界面文件，无宿主重启、后端修改或配置迁移；仍为2连接/0数据集/0拓扑，无凭据属性泄露。原用户标签页未刷新，未调用大模型。

证据：`C:\工作\deepseektest\插件测试证据\database_注释复制_20260827`。开发前源码及安装包双备份含时间戳 `20260827_170221`，见交付清单。
