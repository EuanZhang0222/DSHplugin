# 数据库统一语义层插件 Design QA（设计质量验收）

## 2026-08-28 全量分页专项

- 入口保持“数据集详情 → 浏览数据”，增加完整分页器：总条数/总页数、数字页码/省略号、输入页码跳转、页大小、上一页/下一页；另有重新查询、采集进度及关闭释放操作。采集期间每秒刷新状态，完成后自动显示总页数，发生错误停止轮询。
- 200不再代表总结果上限；页内容较长时自动减少条数，超长行提供无损分片，错误与资源限制明确显示。
- MySQL测试数据1507条，实际浏览8页至末页；ClickHouse完成第一页/第二页与刷新。表格独立滚动，翻页后回到该页顶部；短视口按剩余高度进一步收缩表区。
- 浅色/深色截图已检查。CSS视口2851×1603、1910×1269、1134×1045、687×657，未发生页面或弹窗正文横向溢出；宽表在表区内部滚动。测试视口已恢复，主题已恢复浅色。
- 最终完整分页器验证：MySQL直接跳第8页/第3页，修改50行后31页且末页7条，越界页码提示；ClickHouse深色主题第8/16页正常。687px宽下页码和输入跳转可见，无正文横向溢出。
- 正式能碳皮肤真实读取能源基础信息表9条，显示1页。浏览器功能错误0；隔离主动重启时5条连接重试警告，正式新页0错误/0警告。证据在`插件测试证据/database_全量分页_20260828`。不把截图请求的物理尺寸冒充CSS视口。

## 2026-08-27 按需语义预览专项

- 入口不变，预览新增“按问题预览/完整详情”两种模式，默认先输入问题，不立即展开整表全文。
- 修复旧单数据集卡片被长字段内容拉高的问题：正式实测左目录高197px、右结果高1409px，各自按内容排版。
- 原始定义、重复说明来源和结构化全文按需展开；目录8条/页、完整字段12条/页、补选字段10条/页。短问题、范围和预算主操作集中在结果上方。
- 实测目录翻页、完整18字段第二页、字段补选、状态保留、缺少问题校验、超预算阻断和未配置模型时的计划可见/执行禁用。
- 隔离浅色与深色、正式能碳皮肤均通过截图检查。当前1910px视口无页面/弹窗正文横向溢出，未新增硬编码配色覆盖宿主皮肤。
- 错误日志：隔离0、正式0。证据目录：`插件测试证据/database_语义按需上下文_20260827`。未测试的窄视口不宣称通过。

以下为原有页面历史验收，主体连接/数据集/拓扑操作沿用此前原型。

验收日期：2026-08-25  
参考原型：`C:\工作\deepseektest\插件设计文件\数据库数据集与语义层原型\数据库数据集与语义层交互原型.html`  
真实实现：DeepSeek Harness 独立 web profile（网页运行配置），`http://127.0.0.1:3081/`

## 同视口视觉对照

- 在同一个 In-app Browser（应用内浏览器）窗口、相同视口下分别截取参考原型与真实插件的数据集页。
- 真实插件沿用原型的顶部三页签、标题与主操作、三项统计卡、搜索/工具栏、数据集表格和底部语义上下文提示结构。
- Harness 原生设置导航替代原型中的模拟“我的插件”侧栏；数据条数与文案来自真实测试数据，不复制原型演示数据。
- 设置面板在数据库插件打开时扩展到最大 1500 像素，并保留视口边距，解决默认 800 像素面板造成的拥挤和横向裁切。

## 交互验收

- 真实创建 MySQL（关系型数据库）连接并完成“测试连接”；创建后页面只显示凭据已配置状态。
- 从真实 MySQL 协议测试库读取数据库、表、表注释、字段名、独立字段类型、字段注释、索引和外键。
- 成功创建“电表采集明细”和“计量设备档案”两个数据集，并维护表用途、业务名称、自定义注释和敏感字段。
- 成功创建“能源计量主题拓扑”，规则引擎识别 `meter_reading.meter_id -> meter.id` 为 100% 外键候选，人工确认后才进入语义上下文。
- 上下文预览可切换数据集，只展示已确认关系，并同时标注连接名、数据库名、表名、字段名和数据集业务名。
- 新建数据集来源连接加载失败与语义层版本不匹配已分离处理，不再因为一个 404 响应静默清空连接列表。

## 皮肤与可访问性

- 浅色、深色与跟随系统均使用 Harness 设计变量，不写死页面背景或正文颜色。
- 主按钮前景与主题主色保持对比；图标使用 Harness 自带图标库。
- 图标按钮补充可访问名称和悬浮提示；关键表单可通过标签名称定位。
- 小视口下连接卡片、统计卡、表单、语义预览和拓扑检查器切换为单列；宽表仅在自身容器内横向滚动。

## 2026-08-27 弹窗宽高自适应回归

本轮只修改数据库插件布局，并在正式 `http://127.0.0.1:3080/` 的“我的插件”弹窗验证。原有 2 条连接继续显示，未新增或修改数据库、数据集与关系数据。

- 原因：连接管理和数据集共用的页面容器被限制为 1250 像素；拓扑页单独取消了限制。现在三页共用全宽规则。
- 宽度断点改用插件实际内容区尺寸；不要求用户同时缩小整个浏览器。
- 高度由宿主内容区管理，内容短时自然排版、内容长时纵向滚动，不拉长卡片或固定页面高度。

| 弹窗尺寸（宽×高，像素） | 连接管理 / 数据集页面宽度 | 验证结果 |
|---|---|---|
| 1681×860 | 1437 / 1437 | 占满可用内容宽度；连接双列、统计三列 |
| 1231×760 | 987 / 987 | 保持自适应，工具栏与卡片没有横向溢出 |
| 731×560 | 487 / 487 | 窄内容区自动单列，长内容纵向滚动 |
| 681×460 | 数据集 649 | 宿主导航改为顶部，数据集按增加后的内容宽度重排；拓扑也通过 |
| 381×360 | 349 / 349 | 小弹窗依旧可滚动访问按钮；无根页面横向溢出 |
| 1681×460 | 1429 / 1429 | 仅减小高度时显示内容滚动条，不裁断底部功能 |

- 表格的 930 像素最小宽度只在自身容器内滚动，未撑宽弹窗或页面。
- 窄弹窗内“新建连接”表单仍相对视口居中，不受响应式容器裁切；测试未填写或提交连接信息。
- 浅色、深色均目视检查；测试后已恢复原来的“跟随系统”和 1681×860 弹窗尺寸，关闭重开后尺寸一致。
- 13 项核心语义回归全部通过；构建通过；本次浏览器捕获错误为 0。
- 正式副本仅更新 `lib/client.js` 与其源码映射文件，刷新即生效，未重启宿主或修改其他插件。
- 截图与布局测量：`C:\工作\deepseektest\验证记录\数据库自适应布局_20260827`。
- 回滚备份：`C:\工作\deepseektest\插件备份\database_自适应布局修复前_20260827_1350.zip`；可从备份恢复 `lib/client.js` 后刷新页面。

## 2026-08-27 数据来源逐级选择回归

- 新建数据集使用“来源连接 → 来源数据库 → 来源表”三个逐级下拉框，不再要求手填库名或表名。打开后自动读取所选连接有权限访问的库目录；选库自动列出表，选表自动读取字段定义。
- 已配置默认库且确实可访问时自动选中；仅一个可见库时自动展开；多个库时保留用户选择。浏览数据页预选的表仍会经过真实目录验证。
- 切换上级来源会清空下级选项和旧字段；请求取消及版本校验防止慢响应覆盖新选择。同表刷新保留自定义业务语义，同时更新真实字段类型、数据库注释和索引信息。
- 加载中、空库、目录失败、字段读取失败分别显示明确状态及重试入口；元数据未加载成功时不能保存，不生成演示字段。
- 隔离 3081 页面通过实际驱动访问本机 MySQL 协议服务和 ClickHouse HTTP（网页协议）服务：分别自动读取并保存 5 字段、3 字段数据集；验证空库、切换、刷新保留业务注释、连接中断及恢复后重试。协议服务仅用于验收，不冒充 UAT 数据库。
- 正式 3080 页面经原有 MySQL 连接读取 3 个真实库；`ems_betai` 返回 484 张表，`topv-prod` 返回 16 张表，选择 `devices_default` 后自动显示 5 个字段。只读取结构元数据，没有读取业务数据行或保存正式数据集。
- 新发现并修复连字符库名无法读取字段的问题；标识符仍经过白名单校验并分别加引号，反引号、分号、路径分隔符等危险输入仍被拒绝。
- 深色/浅色实际检查通过；正式“我的插件”宽 731 像素时内容区 487 像素，来源选择自动单列，根容器滚动宽度等于可用宽度，无页面级横向溢出；完成后恢复 1681×860 原弹窗尺寸。
- 25 项自动化测试通过、浏览器捕获错误 0；MySQL 协议集成仍通过（2 个数据集、8 个字段、1 条确认关系、1 行测试查询）。
- 正式副本同步浏览器代码及连字符兼容修复，并重启宿主；原有 2 条连接、0 个数据集保持不变。
- 证据：`C:\工作\deepseektest\验证记录\数据库级联选择_20260827`。
- 开发前备份：`C:\工作\deepseektest\插件备份\database_级联选择开发前_20260827.zip`；恢复备份中的浏览器代码与 `lib/semantic/query.js` 后需要重启宿主。

## 2026-08-27 必填标注与保存提示回归

- 数据集名称、来源连接、来源数据库、来源表均展示文字“* 必填”，输入元素同时带有原生必填属性；表的业务作用明确标为选填，字段业务语义也可以后补。
- 空表单点击保存一次列出四个缺项，四个输入框标红并定位到名称；填入名称后只保留三个来源提示；点击汇总中的某一项可直接定位对应控件。
- 新建数据集已读出 3 个字段但名称为空时，只提示“请填写数据集名称”，保存按钮仍可点击；编辑数据集同样通过名称必填检查。
- 独立 3081 通过本机 MySQL 协议服务创建数据集，仅填写名称与来源，表用途和所有字段业务语义为空仍成功保存；接口核对只有一条新建测试数据集。
- 编辑测试数据集时停止本机测试数据库，产生真实连接拒绝：显示保存失败原因，名称和表用途完整保留，按钮恢复可重试；恢复服务后更新同一数据集成功，未新增重复数据集。
- 浅色/深色目视验收通过；正式“我的插件”弹窗在 1681×860、1231×860、731×860 下内容宽分别为 1437、987、487 像素，滚动宽度均与可用宽度一致。窄窗口选择器单列，长表单纵向滚动，提示文字未撑宽页面。
- 正式页面使用新开的验收标签页，未刷新用户已有草稿页；测试后恢复原 1681×860 弹窗尺寸。正式接口仍为 2 条连接、0 个数据集、0 个拓扑，未返回用户名/密码。
- 34 项自动化测试通过（新增 9 项保存校验），两个验收标签页捕获浏览器错误均为 0；只同步正式浏览器代码及映射文件，没有改动后端或重启正式宿主。
- 证据：`C:\工作\deepseektest\验证记录\数据库保存校验_20260827`。
- 回滚备份：`C:\工作\deepseektest\插件备份\database_保存校验修改前_20260827_1632.zip`。恢复其中界面文件后刷新即可，不需要回滚连接配置。

## 2026-08-27 数据库注释一键复制回归

- 在字段定义区新增轻量工具条，提供默认仅填空白/覆盖已有内容方式，以及两个复制目标按钮；保存后仍沿用已有字段编辑表格，不改原页面布局与字段类型独立列。
- 默认模式保留人工值；覆盖显示将修改及覆盖的数量，取消不修改，确认后结果显示成功与跳过数量。无注释或超长不覆盖旧内容，超长可以展开查看字段名和长度。
- 未读字段和读取期间按钮禁用；切换来源后旧复制结果与模式重置。两列操作彼此隔离，复制后可手工修改，刷新同表和保存重开保持内容，启用/敏感标记不变。
- 新建与编辑真实测试数据集均通过；正式18字段表复制17项、跳过1项缺注释，两个目标都经过页面操作。正式没有保存测试数据，原用户草稿页未刷新。
- 浅深色截图通过；正式弹窗1681/1231/731像素宽度下内容区1437/987/487像素，滚动宽度相同，工具条自动换行且按钮可用。最后恢复1681×860尺寸。
- 45项规则/目录/校验/复制测试通过；隔离及正式页浏览器错误均为0；只更新正式界面文件，不重启宿主。
- 证据：`C:\工作\deepseektest\插件测试证据\database_注释复制_20260827`。
- 回滚：恢复 `database_注释复制开发前_20260827_170221.zip` 中的界面文件后刷新，不回滚或覆盖连接配置。

final result: passed
