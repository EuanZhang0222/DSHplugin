# 2026-09-07 正式部署同步记录

## 本次发布

以本机正式部署为基准同步五个插件，并提供 [统一安装指南](INSTALL.md)、[智能体执行入口](AGENTS.md)、[当前安装包与摘要](releases/manifest.json)。不含用户数据库配置、真实凭据、技能内容、会话或本机备份。

| 插件 | 当前版本 | 本次重点 |
|---|---|---|
| 我的插件 | 1.0.0 | 保留正式部署的聚合面板与尺寸记忆，重新打包核对 |
| 技能管理器 | 1.0.0 | 保留正式部署的技能管理、批量迁移能力，重新打包核对 |
| 皮肤管理器 | 1.2.0 | 能碳章鱼、格创东智品牌资源与界面适配 |
| API 调用 | 1.0.2 | 真实 cURL（命令行请求）导入、超长整数保真、新宿主工具注册与合法 JSON（数据格式）返回、响应体超时和取消 |
| 数据库统一语义层 | 2.1.0 | 数据集、语义与人工确认关系、按需检索、全量分页、字段注释复制及界面源码 |

数据库宿主文件额外同步了正式副本已有的字符串设置命名空间适配，避免依赖新宿主已不提供的 settingsNamespace（设置命名空间辅助函数）。版本号沿用当前部署；2.1.0 包应以本次摘要识别，不能复用旧缓存包。API 1.0.2 也包含正式部署的新宿主适配。

## 本轮验证

- 五个 npm（包管理）安装包逐文件与源码比较通过；37个运行代码和资源文件与正式部署比较通过（文本换行符标准化后比较）。
- 数据库现有测试82项、API现有测试10项、皮肤现有测试16项，合计108项通过，无失败或跳过。API测试使用实际安装的 DSH 0.1.3-alpha.1 工具编译器。
- 校验安装包内 package.json（包描述）版本、文件清单与 SHA-256（摘要），未打入 node_modules（依赖目录）、用户设置或会话。
- 检查了待发布源码和文档中的高置信度密钥格式，未发现匹配；测试里的 fixture（样例）账号用于本地模拟。
- 安装命令与本机 DSH CLI（命令行）源码核对，使用目标运行配置内的包管理与插件层登记机制。

本轮执行的是源码、安装包、正式文件一致性与自动化回归验证，没有重新安装或重启生产宿主，也没有对其他操作系统执行全新安装或浏览器视觉回归。其他机器安装后仍须执行 INSTALL.md 第5节的实际验收。

## 维护提醒

releases（发布目录）是当前安装入口；子目录中原有1.0.0包留作历史资料。后续修改需重新生成对应包并更新发布清单摘要，提交源码与产物。数据库完整查询必须消费游标至结束，自动识别关系不能绕过人工确认；API导入导出只迁移凭据引用。
